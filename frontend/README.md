# Social Profile - Vue 3 приложение

Красивое приложение профиля пользователя на Vue 3 с функциональностью входа, регистрации и редактирования профиля.

## 📋 Содержание

- [Запуск проекта](#запуск-проекта)
- [Структура проекта](#структура-проекта)
- [Компоненты](#компоненты)
- [Хранилище данных](#хранилище-данных)
- [Как это работает](#как-это-работает)

## 🚀 Запуск проекта

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск dev-сервера
```bash
npm run dev
```

Приложение откроется на `http://localhost:5173`

### 3. Сборка для production
```bash
npm run build
```

## 📁 Структура проекта

```
src/
├── components/          # Vue компоненты
│   ├── AuthForm.vue    # Форма входа/регистрации
│   ├── UserProfile.vue # Страница профиля
│   └── EditProfileModal.vue # Модальное окно редактирования
├── stores/             # Pinia хранилище
│   └── userStore.js    # Store для управления пользователем
├── assets/             # Статические файлы
├── App.vue             # Корневой компонент
├── main.js             # Точка входа
└── style.css           # Глобальные стили (Tailwind)
```

## 🎨 Компоненты

### AuthForm.vue - Форма входа/регистрации
**Назначение:** Отображает интерфейс для входа и регистрации пользователя.

**Основные возможности:**
- Переключение между вкладками "Вход" и "Регистрация"
- Валидация полей формы
- Функции `handleLogin()` и `handleRegister()` для авторизации

**Как работает:**
```vue
// Когда пользователь вводит данные и нажимает "Войти"
const handleLogin = () => {
  userStore.login(loginForm.value.email, loginForm.value.password)
  // User store создает объект пользователя и сохраняет в localStorage
}
```

### UserProfile.vue - Страница профиля
**Назначение:** Отображает информацию профиля пользователя в стиле Twitter/X.

**Основные элементы:**
- Аватар с градиентным фоном
- Основная информация: имя, юзернейм, био
- Метаданные: локация, дата регистрации
- Кнопка "Изменить" для редактирования профиля
- Кнопка "Выход" для логаута

**Структура профиля:**
```
┌─────────────────────────────────┐
│       Градиентный фон           │
│    ┌──────────┐                 │
│    │  Аватар  │  [Изменить]    │
│    └──────────┘                 │
│  Имя пользователя               │
│  @username                      │
│  Мое описание...                │
│  📍 Локация | 📅 Дата          │
│                                 │
│  Email: user@email.com          │
│  О себе: Текст профиля          │
└─────────────────────────────────┘
```

### EditProfileModal.vue - Модальное окно редактирования
**Назначение:** Модальное окно с формой для редактирования данных профиля.

**Элементы форм:**
- Поле для имени
- Поле для email
- Текстовое поле для "О себе" (био)
- Поле для локации

**Особенности:**
- Backdrop с размытием (backdrop-blur)
- Закрытие по клику на backdrop
- Кнопки "Отмена" и "Сохранить"

## 💾 Хранилище данных

### userStore.js - Pinia Store
Это центральное место для управления состоянием пользователя.

**Состояние (State):**
```javascript
user: {
  id: number,
  name: string,
  email: string,
  bio: string,
  registrationDate: Date,
  location: string,
  avatar: string  // URL аватара
}
isAuthenticated: boolean
```

**Методы:**

1. **`login(email, password)`**
   - Имитирует вход в систему
   - Создает объект пользователя с тестовыми данными
   - Сохраняет в localStorage

2. **`register(name, email, password)`**
   - Создает новый аккаунт
   - Генерирует уникальный ID
   - Сохраняет данные в localStorage

3. **`updateProfile(updates)`**
   - Обновляет данные профиля
   - Сохраняет изменения в localStorage

4. **`logout()`**
   - Удаляет данные пользователя
   - Очищает localStorage

5. **`loadUser()`**
   - Загружает пользователя из localStorage при инициализации
   - Восстанавливает сессию при перезагрузке страницы

## 🎯 Как это работает

### Поток аутентификации:
1. Пользователь открывает приложение
2. `App.vue` загружает store при монтировании: `userStore.loadUser()`
3. Если пользователь не авторизован → показывается `AuthForm`
4. Пользователь заполняет форму и нажимает "Войти" или "Зарегистрироваться"
5. Store сохраняет данные в localStorage и устанавливает `isAuthenticated = true`
6. Vue перерисовывает экран и показывает `UserProfile`

### Поток редактирования:
1. Пользователь нажимает кнопку "Изменить"
2. Открывается `EditProfileModal` с текущими данными
3. Пользователь изменяет поля
4. Нажимает "Сохранить"
5. `UserProfile` вызывает `userStore.updateProfile()`
6. Store обновляет данные и сохраняет в localStorage
7. Профиль обновляется на экране

## 🎨 Стили и Tailwind CSS

### Цветовая схема:
- **Основной цвет:** `primary` - `#1DA1F2` (Twitter Blue)
- **Вторичный:** `secondary` - `#657786` (Gray)
- **Темный режим:** поддерживается через `dark:` префиксы

### Используемые компоненты Tailwind:
- `flex`, `grid` - раскладка
- `rounded-lg`, `rounded-full` - скругленные углы
- `border`, `shadow` - бордеры и тени
- `hover:`, `transition` - интерактивные элементы
- `dark:` - темный режим

## 📝 Данные пользователя

После входа/регистрации пользователь получает:
```javascript
{
  id: 1,
  name: "John Doe",
  email: "john@example.com",
  bio: "Web Developer | Vue Enthusiast",
  registrationDate: Date,
  location: "San Francisco, USA",
  avatar: "https://api.dicebear.com/7.x/avataaars/svg?seed=John"
}
```

Аватары генерируются автоматически через API `dicebear.com`.

## 🔗 Готово для Backend

Структура компонентов готова для интеграции с FastAPI backend:

**Что нужно изменить:**
1. В `userStore.js` - заменить `login()` на реальный API вызов
2. Добавить axios или fetch для API запросов
3. Изменить хранилище данных с localStorage на реальные запросы на сервер

**Пример интеграции:**
```javascript
const login = async (email, password) => {
  const response = await fetch('http://localhost:8000/api/login', {
    method: 'POST',
    body: JSON.stringify({ email, password })
  })
  const userData = await response.json()
  user.value = userData
  isAuthenticated.value = true
}
```

## 💡 Советы

- Все данные локально хранятся в `localStorage`, перезагрузите страницу чтобы проверить сохранение
- Для разработки используйте `npm run dev`
- Адреса почты и пароли - тестовые, валидация не проверяется
- Аватары генерируются автоматически на основе имени пользователя

## 🚀 Будущее расширение

Готовые точки расширения:
- [ ] Интеграция с FastAPI backend
- [ ] Загрузка собственного аватара
- [ ] Добавление постов/твитов
- [ ] Система лайков и комментариев
- [ ] Темный режим переключатель
- [ ] Поиск пользователей
- [ ] Подписки на пользователей

---

**Автор:** Создано с Vue 3 + Tailwind CSS + Pinia
